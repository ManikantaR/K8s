#set the below command in ~/.vimrc
autocmd FIleType yaml setlocal sts=2 ts=2 sw=2 expandtab nu rnu ai 
set paste

export do="--dry-run=client -o yaml"
export kdp="kubectl delete --grace-period=0 --force"
export kgp="kubectl get pod"
export d="--dry-run=client"
export oy="-o yaml"

#set auto kubectl auto complete
source <(kubectl completion bash)
echo "source <(kubectl completion bash)" >> ~/.bashrc

alias k=kubectl
complete -F __start_kubectl k


